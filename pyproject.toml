[project]
name = "groundpatrol"
version = "0.1.0"
description = "A governed coastal-operations agent for the Agents for Humans hackathon"
requires-python = ">=3.10"
dependencies = [
  "strands-agents>=1.0.0"
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
